# Energy LSP Extension

This VS Code extension integrates with your Energy-Aware Compiler via an LSP server.

## Features

- Auto-compile on save
- Shows energy usage for every run
- Shows compiler diagnostics
- Adds "Run with Energy" button (CodeLens)
- Works for all major languages

## Requirements

Run the compiler API:

